services:

  tailscale:
    image: tailscale/tailscale:latest
    container_name: qbittorrent-tailscale
    hostname: qbittorrent
    networks:
      - proxy
    environment:
      - TZ=America/New_York
      - TS_AUTHKEY=${TS_AUTHKEY}
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_SERVE_CONFIG=/ts-serve.json
      - TS_USERSPACE=false
      - TS_ENABLE_HEALTH_CHECK=true
      - TS_LOCAL_ADDR_PORT=127.0.0.1:41234
    volumes:
      - ${DATA_NVME_PATH}/qbittorrent/tailscale/config:/config
      - ${DATA_NVME_PATH}/qbittorrent/tailscale/state:/var/lib/tailscale
      - ./ts-serve.json:/ts-serve.json:ro
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:41234/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: always
    pull_policy: always

  application:
    image: lscr.io/linuxserver/qbittorrent
    container_name: qbittorrent-app
    networks:
      - proxy
      - media
    restart: always
    pull_policy: always
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
      - WEBUI_PORT=8080
    volumes:
      - ${DATA_NVME_PATH}/qbittorrent/app:/config/qBittorrent
      - ${DATA_PATH}/medias/events:/downloads/events
      - ${DATA_PATH}/medias/series:/downloads/series
      - ${DATA_PATH}/medias/movies:/downloads/movies
      - ${DATA_PATH}/medias/incomplete:/downloads/incomplete
      - ${DATA_PATH}/medias/seedcrap:/downloads/seedcrap
      - ${DATA_PATH}/medias/torrents:/downloads/torrents
      - ${DATA_PATH}/medias/switch:/downloads/switch
      - ${DATA_PATH}/medias/books:/downloads/books
      - ${DATA_PATH}/medias/qbittorrent:/downloads/qbittorrent
    depends_on:
      tailscale:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "pgrep", "-f", "qbittorrent"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  nginx:
    image: nginx:alpine
    container_name: qbittorrent-nginx
    networks:
      - proxy
      - uptime_kuma
    environment:
      - TZ=America/New_York
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      application:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "pgrep", "-f", "nginx"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    restart: always
    pull_policy: always

networks:
  proxy:
  media:
    external: true
  uptime_kuma:
    external: true
